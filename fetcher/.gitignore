node_modules/
dist/
.env
.env.local
# raw sample dumps from the probe — never commit unofficial-API payloads
.probe-samples/
# local secrets (cookies, tokens) — never commit
.secrets/
*.log
.DS_Store
# generated snapshot artifacts (published to Cloudflare KV/R2, not committed)
snapshot/
